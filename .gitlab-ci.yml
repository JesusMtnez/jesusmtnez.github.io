image: python:3.6-alpine

pages:
  script:
    - apk add --no-cache --virtual git
    - git clone --branch v2.1.0 https://github.com/alexandrevicenzi/Flex.git /tmp/flex-pelican-theme -q
    - pip install -r requirements.txt
    - pelican -s publishconf.py
  artifacts:
    paths:
      - public/

